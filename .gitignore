# Secrets — never commit
.env
.env.local
*.pem
*_credentials.json

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# Data — all pulls and derived outputs are regenerable; keep them out of git
data/
*.parquet
*.csv.gz

# AWS / cloud
.aws/
*.tfstate
*.tfstate.backup

# Editor / OS
.idea/
.vscode/
*.swp

# internal design working notes, not part of the deliverable
.calm-design/

# recorded media / generated video assets (large, not part of the repo)
*.mov
*.mp4
*.wav
*.mp3
video_build/

# stray local working files (not part of the deliverable)
_to_delete/
s3opt_*.jpg
s3opts.png
